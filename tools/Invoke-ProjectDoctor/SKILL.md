# Invoke-ProjectDoctor

## Purpose
Orchestrate existing diagnostic helpers into one project-health report.

## Entry point
`scripts/Invoke-ProjectDoctor.ps1`

## Behavior
Prefer composition over duplicated logic. Read-only by default; report findings and recommended next actions without modifying the project.
