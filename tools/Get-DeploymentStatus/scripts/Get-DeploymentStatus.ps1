function Get-DeploymentStatus {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory, Position=0)]
        [string]$Repository
    )

    throw 'Get-DeploymentStatus scaffold created; implementation pending.'
}
