# Get-DeploymentStatus

## Purpose
Compare local, GitHub, and deployment state for a project.

## Entry point
`scripts/Get-DeploymentStatus.ps1`

## Behavior
Read deployment metadata and commits without triggering deployments. Return source SHAs, status, URLs, and sync state.
