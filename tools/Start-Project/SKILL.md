# Start-Project

## Purpose
Start a local project using the correct package manager and dev script.

## Entry point
`scripts/Start-Project.ps1`

## Behavior
Resolve the project first, detect tooling from repository files, and avoid changing dependencies unless explicitly requested.
