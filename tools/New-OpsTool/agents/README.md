# Agents

Use this folder only when a tool has an actual agentic workflow, agent instructions, or delegated execution contract.

Do not add `agents/` by default just because a tool may interact with AI. Create it with `New-OpsTool -Agents` or as part of `-Extended`.
