# Prompts

Use this folder only when a tool needs reusable LLM prompts.

`New-OpsTool -Prompts` creates this capability folder for a new tool. `-Extended` creates it together with the rest of the extended standard.
