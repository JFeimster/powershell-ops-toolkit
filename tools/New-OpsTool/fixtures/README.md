# Fixtures

Use this folder for deterministic test data, sample project trees, mocked config, and other repeatable inputs used by tests.

Create it with `New-OpsTool -Fixtures` or as part of `-Extended`.
