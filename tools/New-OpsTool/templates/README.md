# Templates

Use this folder for reusable output or file templates that belong to a tool.

`New-OpsTool -Templates` creates this capability folder for a new tool. `-Extended` creates it together with the rest of the extended standard.
