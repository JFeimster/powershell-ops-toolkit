# Get-SkillInventory

## Purpose
Inventory and validate reusable skills/tools under the configured local skills root.

## Entry point
`scripts/Get-SkillInventory.ps1`

## Output
Return skill name, path, exposed commands, structural checks, documentation status, and conflicts.
