function Get-WorkQueue {
    [CmdletBinding()]
    param(
        [Parameter(Mandatory, Position=0)]
        [string]$Repository
    )

    throw 'Get-WorkQueue scaffold created; implementation pending.'
}
