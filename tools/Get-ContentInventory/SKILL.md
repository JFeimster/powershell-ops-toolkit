# Get-ContentInventory

## Purpose
Build a normalized inventory of content and reusable conversion assets across supported sources.

## Entry point
`scripts/Get-ContentInventory.ps1`

## Output
Return structured records with source, asset type, title, URL/path, status, and canonical identifiers where available.
