# Get-ProjectStatus

## Purpose
Return a structured snapshot of local project and Git status.

## Entry point
`scripts/Get-ProjectStatus.ps1`

## Output
Prefer a PSCustomObject so results can be formatted, exported, or consumed by other toolkit commands.
