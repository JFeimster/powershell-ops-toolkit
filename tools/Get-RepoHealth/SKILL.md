# Get-RepoHealth

## Purpose
Run read-only repository diagnostics and return actionable health findings.

## Entry point
`scripts/Get-RepoHealth.ps1`

## Output
Return structured checks with severity, finding, evidence, and recommended action.
