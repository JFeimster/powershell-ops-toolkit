# Open-ProjectStack

## Purpose
Open a project's common local and web surfaces as one operator action.

## Entry point
`scripts/Open-ProjectStack.ps1`

## Dependencies
Reuse local project and link resolution helpers rather than duplicating lookup logic.
