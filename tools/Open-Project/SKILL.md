# Open-Project

## Purpose
Open the best local project match from a repo identifier.

## Entry point
`scripts/Open-Project.ps1`

## Dependencies
Prefer `Find-LocalRepo` for local resolution. Keep external actions limited to opening local apps or URLs explicitly requested by the caller.
